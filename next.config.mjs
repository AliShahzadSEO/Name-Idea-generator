/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
// Add this section to enable Google Analytics
analytics: {
    google: G-CR7P6GW9K1,
  },
};

export default nextConfig;
